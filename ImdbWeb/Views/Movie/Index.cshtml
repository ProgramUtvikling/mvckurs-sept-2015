@model IEnumerable<Movie>


<h2>Liste over filmer</h2>

<ul>
@foreach (var movie in Model.OrderBy(m => m.Title))
{
	<li>
		@Html.ActionLink(movie.Title, "Details", "Movie", new { id = movie.MovieId }, new { @class = "some-css-class" })
	</li>
}
</ul>
